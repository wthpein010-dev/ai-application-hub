name: Build Codex Confirmation Bar v2 (compatibility entry)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release-v2:
    permissions:
      contents: write
    uses: ./.github/workflows/build-codex-confirmation-bar.yml
