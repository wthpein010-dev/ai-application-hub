# Paws 公开内置关卡库清空实施计划

日期：2026-07-28

1. 先把发布目录测试改为要求空索引、目录仅保留 `index.json`，运行并确认旧实现失败。
2. 删除 23 个公开关卡 JSON，写入空索引。
3. 修改静态页说明与控制器空态，区分“真空库”和“搜索无结果”。
4. 将静态 API 单测与真实发布数据解耦，使用内联 bundled fixture 保留 bundled 覆盖。
5. 调整浏览器回归：先验收空库，再导入测试 JSON，继续验证编辑、AI、2D/3D、试玩和本地删除。
6. 更新 Hub 卡片、教程脚本、字幕、录屏与证明。
7. 运行 Paws 全量单测、语法检查、两个静态浏览器门禁和 LAN 门禁；完成代码与视觉复核。
8. 提交后快进更新 `origin/main`，等待精确 SHA 的 Pages 部署并做公网 HTTP/浏览器验收。
9. 将已确认结果更新到长期项目记忆。
