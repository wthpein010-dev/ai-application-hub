# Paws 模板塔群生成与受压视觉实施计划

日期：2026-07-29

1. 先增加失败测试：随机类型允许逐层奇数但全局必须为偶数；统计保留层模板、类型比例和盲盒栈。
2. 增加失败测试：200/15 输出以 `type=-1` 为主体、逐层非均匀、同层零重叠、存在合规盲盒栈且不是整板大平层。
3. 增加失败测试：普通受压牌按 0.58 压暗；盲盒背面不重复叠加黑罩；平铺下层随覆盖自动翻回和翻开。
4. 扩展统计学习结构，并用参考模板逐层序列取代平均铺层分配。
5. 实现模板轮廓变换、局部安全补点、盲盒栈和 `type=-1` 主体输出，更新算法版本为 v9。
6. 扩展非第一关随机分配的多候选可解门禁，并在试玩初始化与覆盖刷新时同步盲盒正反面。
7. 运行 Paws 单元测试、语法检查和多种子压力回归，修复所有确定性及性能问题。
8. 本地启动工作台，在 2D/3D 试玩中检查压暗层次、盲盒背面、塔群边界与缺口并保留截图。
9. 复核差异和测试证据，验证 GitHub 写权限，提交并推送到 `origin/main`。
10. 等待对应 SHA 的 GitHub Pages 部署成功，完成公网 HTTP 与浏览器验收，更新长期项目记忆。
